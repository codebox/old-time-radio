<!doctype html>
<html>
    <head>
        <%- include('partials/head', { title: `${episode.show}: ${episode.title}`, description: `Plot summary and episode information for ${episode.show} episode '${episode.title}'` }) %>
    </head>
    <body>
        <%- include('partials/header', { subTitle: `${episode.show}: ${episode.title}`, cssClass: "episodeHeader" }) %>
        <main>
            <%- include('partials/episode-details', episode) %>
        </main>
    </body>
</html>
